import 'package:flutter/material.dart';

class CandidatesCandidatesPage extends StatelessWidget {
  const CandidatesCandidatesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Candidates Details')),
      body: const Center(child: Text('Candidates Details Page')),
    );
  }
}